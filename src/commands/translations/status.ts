import { globalLogger } from '../../utils/logger';

export async function runTranslationsStatus(_args: string[]): Promise<void> {
  globalLogger.warn('`curlydots translations status` is not implemented yet.');
}
