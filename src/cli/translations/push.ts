import { globalLogger } from '../../utils/logger';

export async function translationsPushCommand(_args: string[]): Promise<void> {
  globalLogger.warn('`curlydots translations push` is not implemented yet.');
}
